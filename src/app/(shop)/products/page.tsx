import { titleFont } from "@/config/fonts";


export default function ProductsPage() {
    return (
        <section>
            <h2 className={`${ titleFont.className } font-bold `} > Products page </h2>
        </section>
    )
}
